# Deployed (Cloud Run) dependencies — the root Dockerfile installs THIS file.
# Compatible-release pins (~=) cap major-version breakage while still allowing
# patch/minor security updates. For fully reproducible builds, generate a
# hash-locked file with pip-compile.
google-genai~=1.0                # serving code imports `from google import genai`
google-cloud-aiplatform~=1.60    # vertexai — legacy corpus build (pipeline/embed.py)
google-cloud-storage~=2.18
pymupdf~=1.24
python-docx~=1.1
python-dotenv~=1.0
flask~=3.0
gunicorn>=22.0.0,<24.0.0
