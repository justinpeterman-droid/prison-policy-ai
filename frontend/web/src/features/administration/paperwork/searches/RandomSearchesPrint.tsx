import { SEARCH_SECTIONS, type RandomSearchPayload } from "./model";
export function RandomSearchesPrint({ payload }: { payload: RandomSearchPayload }) { return <article className="random-searches-print" data-testid="random-searches-print"><header><h1>Random Searches Log</h1><span>{payload.work_date} · {payload.shift} Shift</span></header><div>{payload.sections.map((section, index) => <section key={section.section_code}><h2>{SEARCH_SECTIONS[index].label}</h2>{section.blocks.map((block, blockIndex) => <article key={blockIndex} data-testid="random-search-print-block"><h3>Search {blockIndex + 1}</h3><p><strong>Officer:</strong> {block.officer?.display_name_snapshot ?? "—"}</p><p><strong>Date / Time:</strong> {block.search_date ?? "—"} {block.search_time ?? ""}</p><p><strong>Individual Last Name:</strong> {block.individual_last_name || "—"}</p><p><strong>Individual Number:</strong> {block.individual_number || "—"}</p><p><strong>Barracks / Rack:</strong> {block.barracks_rack || "—"}</p><p><strong>Contraband Found and Disposition:</strong> {block.contraband_disposition || "—"}</p></article>)}</section>)}</div></article>; }
