import { useState } from "react";
import { fetchDailyRevisions, restoreDailyRevision, type DailyRecord, type DailyRevision } from "../api";
export function DailyRevisionPanel({record,onRestored}:{record:DailyRecord;onRestored:(record:DailyRecord)=>void}){const[open,setOpen]=useState(false);const[items,setItems]=useState<DailyRevision[]>([]);const[loading,setLoading]=useState(false);const[error,setError]=useState<string|null>(null);const[selected,setSelected]=useState<number|null>(null);async function toggle(){if(open){setOpen(false);return}setOpen(true);if(items.length)return;setLoading(true);try{setItems(await fetchDailyRevisions(record.kind,record.recordId));}catch(reason){setError(reason instanceof Error?reason.message:"Revision history could not be loaded.");}finally{setLoading(false);}}async function restore(){if(selected===null)return;setError(null);try{const restored=await restoreDailyRevision(record.kind,record.recordId,selected);setSelected(null);setItems([]);onRestored(restored);}catch(reason){setError(reason instanceof Error?reason.message:"The revision could not be restored.");}}return <section className="daily-revision-panel"><button type="button" className="admin-secondary-button" onClick={()=>void toggle()}>Revision history</button>{open?<div><h2>Revision history</h2>{loading?<p>Loading revisions…</p>:null}{error?<div role="alert" className="admin-alert error">{error}</div>:null}<ol>{items.map(item=><li key={item.revisionNumber}><div><strong>Revision {item.revisionNumber}</strong><span>{new Date(item.createdAt).toLocaleString()} · editor {item.editorStaffMemberId}</span><small>{item.reason} · {item.changedFields.length?item.changedFields.join(", "):"initial record"}</small></div>{item.revisionNumber!==record.revision?<button type="button" onClick={()=>setSelected(item.revisionNumber)} aria-label={`Restore revision ${item.revisionNumber}`}>Restore</button>:<em>Current</em>}</li>)}</ol></div>:null}{selected!==null?<div className="admin-dialog-backdrop"><section role="dialog" aria-modal="true" aria-label={`Restore revision ${selected}`} className="admin-confirm-dialog"><h2>Restore revision {selected}</h2><p>This preserves current history and creates a new attributed revision from the selected content.</p><div><button type="button" className="admin-secondary-button" onClick={()=>setSelected(null)}>Cancel</button><button type="button" className="admin-primary-button" onClick={()=>void restore()}>Confirm restore</button></div></section></div>:null}</section>}
