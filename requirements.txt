# Local dev / CI and contract-test dependencies (mirror of
# backend/requirements.txt minus the corpus-build-only
# google-cloud-aiplatform). Compatible-release pins (~=) cap major-version
# breakage while allowing patch/minor security updates.
google-genai~=1.0                # Vertex AI Gemini client (from google import genai)
google-cloud-storage~=2.18
google-cloud-tasks>=2.23,<3.0
pymupdf~=1.24
python-docx~=1.1
python-dotenv~=1.0
flask~=3.0
gunicorn>=22.0.0,<24.0.0
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg[binary]>=3.1,<4.0
argon2-cffi>=23.1,<26.0
packaging>=24.0,<26.0
openapi-spec-validator>=0.7,<1.0
pydantic>=2.13,<3.0
