# Local dev / CI dependencies (mirror of backend/requirements.txt minus the
# corpus-build-only google-cloud-aiplatform). Compatible-release pins (~=) cap
# major-version breakage while allowing patch/minor security updates.
google-genai~=1.0                # Vertex AI Gemini client (from google import genai)
google-cloud-storage~=2.18
pymupdf~=1.24
python-docx~=1.1
python-dotenv~=1.0
flask~=3.0
gunicorn>=22.0.0,<24.0.0
